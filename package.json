{
  "name": "plain-todo",
  "version": "1.0.0",
  "description": "Todo application using nothing but plain javascript.",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint && jest",
    "coverage": "jest --coverage",
    "lint": "eslint src webpack.config.js",
    "build": "npm run test && webpack",
    "watch": "npm run test && webpack --watch",
    "ci": "npm run test && npm run coverage"
  },
  "eslintIgnore": ["*.test.js"],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.19.1",
    "eslint-watch": "^3.1.3",
    "jest": "^22.4.3",
    "webpack": "^3.11.0"
  }
}
